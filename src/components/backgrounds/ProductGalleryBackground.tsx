'use client';

import CleanBackground from '../CleanBackground';

const ProductGalleryBackground: React.FC = () => {
  return <CleanBackground variant="shop" />;
};

export default ProductGalleryBackground;
